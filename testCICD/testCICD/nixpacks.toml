[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "dotnet restore ./testCICD.csproj",
  "dotnet publish ./testCICD.csproj -c Release -o out"
]

[start]
cmd = "dotnet out/testCICD.dll --urls=http://0.0.0.0:$PORT"
